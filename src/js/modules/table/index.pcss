@import '~css/variables.pcss';

.table-container {
  /* overflow-x: auto; it's break "position: sticky" in titleLine */
  padding-top: 50px;
  width: 100%;

  & > header {
    display: grid;
    grid-gap: 10px;
    grid-template-columns: repeat(auto-fit, minmax(0, max-content));
    justify-content: flex-end;

    & > button {
    }
  }

  & > header + main {
    margin-top: 20px;
  }

  & > main {
    min-width: max-content;
  }
}
