@import '~css/variables.pcss';

.eventsWrapper {
    & > header {
        background-color: $colorBlueLight;
        padding: 17px $margin-mobile;

        & > h2 {
            color: $colorWhite;
            font-size: $font-size-2;
            text-align: center;
        }
    }

    & > main {
        padding: $margin-mobile;
    }
}